[package]
name = "faggot"
version = "0.1.0"
edition = "2021"

[dependencies]
markdown = "1.0.0-alpha.16"  # markdown-rs
toml = "0.8.21"
axum = "0.7"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.5", features = ["fs"] }

